namespace Atlas.Scanner.Runtime;

/// <summary>
/// Assembly anchor. The runtime will own child-process hosting, resource caps and
/// the job progress protocol; it arrives with the first real scanner.
/// </summary>
public static class AssemblyMarker;
